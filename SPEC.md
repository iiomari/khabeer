# خبير — المواصفة الكاملة

> **كيف تستخدم هذا الملف؟** انسخ كل ما تحت سطر الفاصل والصقه في أي أداة بناء
> (Claude · OnSpace · Cursor · Lovable) لإعادة بناء المنصة من الصفر.
> هذا الملف يصف المنتج **كما هو مبني ومنشور فعلًا**، لا كما خُطّط له.

---

# ابنِ منصة «خبير» — سوق استشارات سعودي يربط المتقاعدين بالمنشآت

## ٠) قواعد غير قابلة للتفاوض

- **عربي بالكامل، RTL حقيقي** (لا ترجمة معكوسة). **كل** النصوص في قاموس واحد `src/lib/i18n/ar.ts` — ممنوع نص مكتوب داخل أي مكوّن، ليسهل إضافة الإنجليزية لاحقًا.
- **تطبيق حقيقي لا صفحة هبوط**: مصادقة فعلية، قاعدة بيانات، أدوار، وكل زر يعمل. **ممنوع زر معطّل أو رابط وهمي.**
- كل خدمة خارجية يتعذّر ربطها (الدفع، نفاذ، نموذج الذكاء الاصطناعي) تُنفَّذ **خلف واجهة قابلة للتبديل** مع تنفيذ تجريبي نظيف موثّق بوضوح.
- كل قائمة تحتاج ست حالات: **تحميل (skeleton) · فارغة · خطأ · نجاح · تحقق من المدخلات · تغذية راجعة (toast)**.
- متجاوب من ٣٢٠px · وضع ليلي كامل · إتاحة (aria، تباين، تنقّل بالكيبورد، `prefers-reduced-motion`).
- الخط: IBM Plex Sans Arabic **محمّل محليًا** (لا CDN)، حجم أساس 17px.

## ١) المشكلة والفكرة

مئات الآلاف من السعوديين يتقاعدون سنويًا ومعهم عقود من الخبرة التشغيلية غير الموثّقة، بينما منشآت صغيرة ومتوسطة تواجه مشاكل يعرف حلَّها هؤلاء تحديدًا ولا تملك ثمن بيت استشاري.

**الفكرة المحورية:** صاحب المنشأة يملك **مشكلة**، لا **مسمى وظيفيًا**. لذلك نعكس اتجاه المنصة: بدل أن يبحث العميل في دليل، يكتب مشكلته بلغته العادية فيقرأها الذكاء الاصطناعي ويرشّح له الخبير المناسب.

**الشعار:** «خبرتك ما توقف عند التقاعد»

## ٢) الأدوار وحماية المسارات

| الدور | من هو | صلاحياته |
|---|---|---|
| `EXPERT` | متقاعد بخبرة ٢٢–٣٥ سنة | ملف مهني، خدمات وأسعار، أوقات توفر، قبول/رفض الحجوزات، أرباح |
| `CLIENT` | منشأة صغيرة/متوسطة أو فرد | كتابة المشكلة، الترشيحات، الحجز والدفع، المراسلة، التقييم |
| `ADMIN` | فريق المنصة | توثيق الخبراء، إدارة المستخدمين والتصنيفات، نبض الطلب |

**Middleware** يمنع الوصول حسب الدور ويعيد التوجيه للوحة الصحيحة، **ويحفظ الرابط المقصود مع الـquery** ليعود إليه المستخدم بعد تسجيل الدخول. هذا ضروري: الزائر الذي يحلّل مشكلته ثم يسجّل الدخول **يجب** أن يعود لنتيجته وليس للوحته.

حالة المستخدم: `ACTIVE` أو `SUSPENDED`. حالة توثيق الخبير: `DRAFT → PENDING → VERIFIED | REJECTED` (مع سبب رفض مكتوب).

---

## ٣) ⭐ الميزة الأولى: المطابقة الذكية (قلب المنتج)

### الصفحة الرئيسية
البطل ليس زرًا بل **صندوق نصي كبير** — ويظهر **للعميل والزائر فقط**. الخبير أو المشرف يرى مكانه بطاقة ترحيب تنقله إلى لوحته (صندوق وصف المشكلة ليس من عمله).

- عنوان: «اشرح مشكلتك، ونحن نجد لك الخبير»
- منطقة نص ٢٠–١٥٠٠ حرف + زر «حلّل مشكلتي وارشّح لي خبراء»
- **٣ أمثلة جاهزة بنقرة واحدة** تملأ النص فورًا (ضرورية للعرض السريع وللجوال):
  1. **ارتفاع تكلفة التوزيع** — *ارتفعت تكلفة التوزيع عندنا ٢٢٪ خلال ستة أشهر وما نعرف السبب. عندنا ثلاثة مستودعات في الرياض وجدة والدمام، ونشحن يوميًا حوالي ٤٠٠ طلب لعملاء تجزئة، والمخزون يتكدس في مستودع جدة بينما ينفد في الدمام.*
  2. **دوران الموظفين** — *شركتنا فيها ٦٠ موظفًا، وخلال السنة الماضية استقال ١٤ منهم أغلبهم من قسم المبيعات. الرواتب قريبة من السوق لكن ما عندنا وصف وظيفي واضح ولا مسار ترقيات.*
  3. **تعثّر مشروع** — *مشروع إنشائي متأخر سبعة أشهر، والمقاول يطالب بتمديد وتكاليف إضافية. الإنجاز الفعلي ٥٥٪ مقابل ٨٥٪ مخطط.*
- رابط ثانوي: «أعرف من أحتاج — تصفّح الخبراء»
- **التحليل متاح بلا تسجيل دخول.** تسجيل الدخول مطلوب عند الحجز فقط.

### أثناء التحليل
حالة تحميل من **ثلاث خطوات متتابعة**، لا دائرة تدور:
`نقرأ المشكلة… ← نحدد المجال والمهارات المطلوبة… ← نطابق خبراء المنصة…`

### صفحة النتيجة `/match/[id]` — أهم شاشة في التطبيق

**العمود الأيمن — بطاقة «ملخص مشكلتك»:**
- شارة المحرك المستخدم (صادقة: «حُلِّل بواسطة Claude» أو «محرك المطابقة الداخلي»)
- **المجال** بشارة وأيقونة، أو «غير محدد»
- **السؤال الذي يجب أن تطرحه** — إعادة صياغة المشكلة كسؤال استشاري حقيقي، مميّز بحدّ جانبي بلون العلامة
- **المهارات المطلوبة** — ٣ إلى ٥ شرائح
- **المدة المقترحة** (٣٠/٤٥/٦٠/٩٠ دقيقة) و**نطاق سعر عادل** بالريال، في بطاقتين
- **«أسئلة اطرحها على الخبير»** — ٤ أسئلة مرقّمة **مخصصة لحالته** تذكر أرقامه وتفاصيله. *هذه اللمسة تحوّل الذكاء الاصطناعي من مصنِّف إلى مستشار — لا تحذفها.*
- «ما كتبته» — النص الأصلي

**العمود الأيسر — «ثلاثة خبراء مرشّحين»:** لكل بطاقة: رقم الترتيب على الصورة · الاسم · شارة توثيق · المنصب السابق والجهة · العنوان المهني · سنوات الخبرة · المدينة · التقييم · **درجة تطابق بنسبة مئوية مع شريط تقدم** · صندوق **«لماذا هو مناسب؟»** بجملة تربط خلفيته الحقيقية بالمشكلة · السعر · زرّا «الملف الكامل» و«احجز معه الآن».

أسفلها: «عرض بقية الخبراء في هذا المجال».

### 🔒 كيف تُبنى المطابقة — لا تُبسّطها

**مرحلتان:**

**١. استرجاع وترجيح حتمي في قاعدة البيانات** (بلا نموذج). اجلب الخبراء `VERIFIED` + `ACTIVE` + لديهم خدمة نشطة، واحسب لكل واحد:

| العامل | الوزن |
|---|---|
| تطابق المجال | **+٤٠** |
| **عدم** تطابق المجال | **−٣٠** |
| تقاطع المهارات مع نص المشكلة | حتى **+٣٥** |
| التقييم | حتى **+١٢** |
| سنوات الخبرة | حتى **+٨** |
| ملاءمة السعر للميزانية | حتى **+٥** |

خذ أفضل **٨** فقط. *العقوبة على المجال الخاطئ ضرورية: خبير ممتاز في مجال آخر يجب ألا يتفوق على خبير أضعف في المجال الصحيح.*

**٢. النموذج يرتّب هؤلاء الثمانية ويكتب سبب كل ترشيح**، ويعيد **٣**.

**ثلاث ضمانات إلزامية:**
- المعرّفات المُعادة **تُتحقق مقابل قائمة الثمانية** — إن جاء معرّف مخترع يُرفض الردّ كليًا ويُستخدم الترتيب الحتمي. **النموذج لا يستطيع اختراع خبير.**
- **درجة التطابق المعروضة هي الحتمية**، لا رقم يخترعه النموذج — فتبقى قابلة للمقارنة بين الطلبات.
- إذا وُجد ٣ خبراء أو أكثر في المجال الصحيح، **لا تُكمل العدد** بخبراء من مجالات أخرى. قائمة قصيرة صادقة أفضل من ثلاث بطاقات إحداها في غير محلها.

### 🛡️ الموثوقية — شرط أساسي

طبقة الذكاء الاصطناعي خلف **واجهة واحدة** فيها تنفيذان:

- **مزوّد النموذج الحقيقي** — يُفعّل تلقائيًا عند وجود مفتاح API في متغيرات البيئة
- **محرك داخلي حتمي بلا إنترنت** — معجم كلمات مفتاحية عربية لكل مجال، مع **تطبيع عربي** يوحّد `أ إ آ ٱ → ا` و`ى → ي` و`ة → ه` و`ؤ → و` و`ئ → ي` ويحذف التشكيل والتطويل. يستنتج المجال، ويشتق **نطاق السعر من متوسط أسعار الخدمات الفعلية في ذلك المجال داخل قاعدة البيانات**، ويستخدم قائمة أسئلة معدّة لكل مجال.

**كل استدعاء مغلّف بمهلة ٨ ثوانٍ + try/catch.** أي فشل — لا مفتاح، انقطاع، مهلة، JSON تالف، معرّف مخترع — **يسقط تلقائيًا إلى المحرك الداخلي** وتتغير الشارة بصدق. **المستخدم لا يرى رسالة خطأ أبدًا.**

**تخزين مؤقت:** بصمة sha256 للنص المُطبَّع؛ نص مطابق خلال ساعة يعيد استخدام نفس التحليل (أرخص، وفوري عند إعادة العرض).

---

## ٤) ⭐ الميزة الثانية: المساعد الشخصي

مساعد دعم صغير، **ليس ميزة رئيسية** — فقاعة في زاوية الصفحة (جهة البداية في RTL) تظهر **لكل مستخدم مسجّل في كل الصفحات**، العامة والداخلية معًا.

**الشكل:** الضغط يفتح لوحة صغيرة (٣٨٤px على الحاسب، بعرض الشاشة على الجوال) بلا مغادرة الصفحة. رسالة المستخدم يمين، الرد يسار، مؤشر كتابة، تمرير تلقائي، وزر «محادثة جديدة».

**الصوت — عبر واجهات المتصفح مباشرة بلا خدمة خارجية:**
- إدخال: `SpeechRecognition` بلغة `ar-SA` — زر ميكروفون يتحول لحالة استماع
- إخراج: `SpeechSynthesis` — زر «استمع للإجابة» تحت كل رد
- عند عدم الدعم أو رفض إذن الميكروفون: رسالة عربية واضحة تطلب الكتابة بدلًا منه

**أسئلة مقترحة تختلف حسب الدور** تظهر عند الفتح:
- عميل: كيف أحجز · ما حالة حجوزاتي · كيف ألغي حجزًا · كيف أعرف أن الخبير موثّق
- خبير: ما حالة توثيق ملفي · كم أرباحي · كيف أضيف خدمة · كيف أضبط أوقات توفري
- مشرف: كم ملفًا ينتظر التوثيق · كم عدد المستخدمين والحجوزات · كيف أعتمد خبيرًا

### 🔒 ما الذي يعرفه المساعد — قواعد صارمة

سياقه من مصدرين **فقط**:
1. **نص ثابت عن المنصة** داخل الكود: الأدوار، خطوات الحجز، حالات الحجز، معنى التوثيق، الإلغاء، الدفع، بناء الملف.
2. **بيانات المستخدم الحالي وحده**، تُجلب قبل كل رد:
   - عميل: حجوزاته وحالاتها ومواعيدها، إجمالي مدفوعاته، رسائله غير المقروءة
   - خبير: حالة توثيقه وسبب الرفض، طلباته المعلّقة، أرباحه، تقييمه
   - مشرف: إحصاءات عامة، ملفات تنتظر التوثيق

**ممنوعات:**
- ❌ **الوصول إلى بيانات مستخدم آخر ممنوع منعًا باتًا.** كل استعلام مُقيَّد بمعرّف المستخدم من الجلسة.
- ❌ **اختراع رقم أو حالة أو موعد ممنوع.** إن لم تكن المعلومة في السياق، يقولها بوضوح ويوجّه إلى الصفحة المناسبة.
- ✅ كل إجابة عن بيانات المستخدم تنتهي **برابط للصفحة المعنية**.
- ✅ الردود بالعربية، جملتان إلى أربع، بنبرة مهنية بسيطة (كثير من المستخدمين متقاعدون).

**الموثوقية:** نفس نمط المطابقة — عند تعذّر النموذج يعمل **مطابق أسئلة شائعة** بالكلمات المفتاحية بعد التطبيع العربي، ويرد بإجابة جاهزة + الرابط المناسب. **المساعد لا يتعطل أبدًا.**

**حد الاستخدام:** ٣٠ رسالة لكل مستخدم في الساعة، مع رسالة لطيفة عند التجاوز.

---

## ٥) الصفحات — ٣٠ صفحة و٤ مسارات API

### عامة (بلا تسجيل دخول)

| المسار | المحتوى |
|---|---|
| `/` | البطل بصندوق المشكلة · إحصاءات حية · شبكة المجالات مع عدد الخبراء · خبراء مميزون · «كيف تعمل» بتبويبين · نبذة · آراء عملاء من التقييمات الحقيقية · أسئلة شائعة · شريط دعوة للانضمام |
| `/match/[id]` | نتيجة التحليل والترشيحات |
| `/experts` | الدليل: بحث نصي · تصفية بالمجال والمدينة والسعر الأقصى وأقل تقييم وأقل سنوات خبرة و«المتاحون فقط» · ترتيب (الأعلى تقييمًا / الأكثر خبرة / الأقل سعرًا / الأكثر استشارات) · ترقيم صفحات · حالة فارغة بزر «مسح الفلاتر» |
| `/experts/[id]` | الملف: الصورة والاسم وشارة التوثيق · العنوان والنبذة · الخبرات · المؤهلات · الشهادات · المهارات · **الخدمات الاستشارية** · أوقات التوفر · التقييمات مع توزيعها على النجوم · زر المفضلة |
| `/auth/login` | بريد وكلمة مرور · **زر نفاذ** · بطاقة الحسابات التجريبية |
| `/auth/register` | اختيار الدور أولًا (بطاقتان)، ثم النموذج · **زر نفاذ** |

### العميل

`/dashboard/client` (إحصاءات ومواعيد قادمة) · `/appointments` · `/consultations` (تصفية بالحالة) · `/messages` · `/favorites` · `/payments` · `/settings` · `/booking/[expertId]` · `/bookings/[id]`

### الخبير

`/expert/onboarding` (بنّاء ٤ خطوات) · `/dashboard/expert` · `/appointments` · `/consultations` · `/services` · `/profile` · `/earnings` (رسم بياني شهري) · `/reviews` · `/messages` · `/settings`

### المشرف

`/admin` (إحصاءات + **نبض الطلب** + طلبات التوثيق + آخر الحجوزات) · `/admin/experts` (اعتماد أو رفض مع سبب) · `/admin/users` (بحث وتصفية وتعليق) · `/admin/bookings` · `/admin/categories`

### API

`api/auth/[...nextauth]` · `api/conversations/[id]/messages` · `api/experts/[id]/slots` · `api/notifications`

---

## ٦) الحجز والدفع

**٥ خطوات:** اختر الخدمة ← التاريخ والوقت ← وصف حاجتك ← مراجعة ← الدفع.

- المواعيد تُحسب من **أوقات توفر الخبير** ناقص المحجوز، لـ**١٤ يومًا** قادمة، بفواصل تساوي مدة الخدمة.
- **منع الحجز المزدوج:** تحقّق أن الموعد ما زال متاحًا **لحظة الدفع**، برسالة واضحة إن حُجز أثناء ذلك.
- **الدفع:** مدى / Apple Pay / بطاقة، مع تحقق من الرقم والانتهاء والرمز، خلف واجهة `PaymentProvider` قابلة للتبديل بـ Moyasar أو HyperPay أو Tap.
- **⚡ القادم من المطابقة:** اختر تلقائيًا الخدمة الأقرب للمدة المقترحة، واملأ الوصف من الملخص، **وابدأ من خطوة الموعد** — فتصير ٥ خطوات **٣**.

**دورة الحياة:** `PENDING → CONFIRMED | REJECTED | CANCELLED → COMPLETED` (المكتملة **تُشتق من مرور وقت الموعد**، لا تُحدَّث بحقل).

عند القبول: **تُفتح محادثة تلقائيًا**. بعد الاكتمال: نموذج التقييم للعميل، ويُعاد حساب متوسط تقييم الخبير.

**صفحة تفاصيل الحجز** تعرض: الحالة والرقم المرجعي والموعد والمدة والسعر والطرف الآخر ووصف الحاجة · أزرار (قبول/رفض للخبير، إلغاء للطرفين) · لوحة المحادثة · نموذج التقييم · إيصال الدفع · و**«ملف التحضير»** للخبير إن جاء الحجز من مطابقة: السؤال الحقيقي والمهارات والأسئلة المقترحة.

## ٧) الرسائل والإشعارات

محادثة لكل حجز مؤكد، برسائل نصية وحالة «مقروء» وتحديث دوري كل بضع ثوانٍ.

جرس في الهيدر بعدّاد غير المقروء و«تعليم الكل كمقروء». الأنواع: طلب حجز · قبول · رفض · إلغاء · **تذكير قبل ٢٤ ساعة** · رسالة جديدة · تقييم جديد · نجاح الدفع · اعتماد الملف · رفض الملف.

## ٨) نموذج البيانات — ١٩ جدولًا

```
User · ExpertProfile · ClientProfile
Experience · Education · Certification · Skill
Category · ExpertCategory · ConsultingService · AvailabilitySlot
ConsultationRequest · Booking · Payment
Conversation · Message · Review · Favorite · Notification
```

**ملاحظات مهمة:**
- القيم المحصورة (الأدوار، الحالات، أنواع الإشعارات) **أعمدة نصية** مع أنواع اتحاد في TypeScript، لا enums في قاعدة البيانات — فإضافة قيمة جديدة لا تحتاج ترحيلًا.
- `ConsultationRequest.clientId` **اختياري** لأن الزائر غير المسجّل يحلّل أيضًا؛ يُربط بالعميل عند الحجز.
- `Booking.requestId` اختياري ويربط الحجز بالتحليل الذي جاء منه.

**المجالات (١٧):** الإدارة · الهندسة · المالية والمحاسبة · الموارد البشرية · القانون · تقنية المعلومات · الأمن السيبراني · إدارة المشاريع · التسويق · المبيعات · التعليم والتدريب · الجودة · سلاسل الإمداد · الصحة · الطاقة · الصناعة · الاستشارات الإدارية

## ٩) الهوية البصرية

| العنصر | القيمة |
|---|---|
| الأساسي | كحلي عميق `oklch(0.34 0.062 255)` |
| المميز | برونزي دافئ `oklch(0.68 0.09 65)` |
| الخلفية | أبيض دافئ `oklch(0.985 0.004 90)` — لا أبيض ناصع |
| مساندة | `brand-soft` فاتح جدًا · أخضر للنجاح · كهرماني للتحذير |
| الحواف | `0.75rem` · ظلال خفيفة جدًا · حدود رفيعة |

**الشعار:** حرف **خ** بخط كوفي، والنقطة التي تجعله «خ» **مرسومة منفصلة بالبرونزي** كعنصر هوية مستقل يتكرر في الشارات والأزرار. الحرف **مسار (outline) مستخرج من الخط**، لا نص حي — فيظهر بنفس الشكل على أي جهاز بلا تحميل خط. الكلمة «خبير» **لا تُكتب بجانبه**؛ الحرف وحده هو الشعار.

**الحركة:** ظهور تدريجي للبطاقات بتأخير متتابع، رفع خفيف عند التمرير، واحترام `prefers-reduced-motion`.

## ١٠) نفاذ (محاكاة)

زر «الدخول عبر نفاذ» في صفحتي الدخول والتسجيل. الضغط عليه **يدخل مباشرة بحساب خبير متقاعد جاهز** بلا خطوات، لتوضيح التكامل الوطني المستقبلي. يُكتب في **دالة واحدة معزولة** موثّقة بوضوح أنها محاكاة، لتُستبدل بالتكامل الحقيقي في موضع واحد.

## ١١) الدعم في الفوتر

عمود «الدعم والتواصل» فيه أوقات العمل (الأحد–الخميس ٩ص–٥م) وأربع قنوات بأيقونات: **واتساب · إكس · إنستقرام · بريد**، مع تنويه صريح أسفل الصفحة أنها حسابات تجريبية للعرض.

## ١٢) البيانات التجريبية — المنصة يجب أن تبدو مأهولة

| العنصر | العدد |
|---|---|
| مستخدمون | ٣٩ |
| خبراء (منهم ٢١ موثّق) | ٢٣ |
| عملاء (شركات وأفراد) | ١٥ |
| مشرف | ١ |
| خدمات استشارية | ٤٤ |
| حجوزات (١٨ مكتملة) | ٤٤ |
| تقييمات (متوسط ٤٫٥٥) | ١٤ |
| تحليلات مشاكل | ٢٣ |
| محادثات / رسائل | ٣٠ / ٥١ |
| مفضلات | ٣٤ |

**قواعد البذر:**
- كل خبير بمسيرة عربية واقعية كاملة: نبذة، ٢–٣ خبرات، مؤهلات، شهادات، ٥ مهارات، خدمتان، أوقات توفر.
- **وزّع الخبراء بحيث يوجد ٣ خبراء على الأقل في كل مجال من مجالات الأمثلة الثلاثة** — وإلا ستكون الترشيحات الثانية والثالثة من مجال خاطئ وتنهار مصداقية المطابقة.
- **تحليل مشكلة واحد بمعرّف ثابت** ليكون رابط عرض مضمون يعمل بلا أي استدعاء.
- كلمة مرور موحّدة، وبطاقة في صفحة الدخول تعرض الحسابات.
- سكربتات البذر **إضافية وقابلة لإعادة التشغيل** (تتخطى الموجود)، لا تمسح قاعدة البيانات.

## ١٣) البنية التقنية

Next.js 16 (App Router · Turbopack · typed routes) · React 19 · TypeScript صارم · Tailwind v4 · shadcn/ui · PostgreSQL (Neon) · Prisma 7 مع driver adapters · Auth.js v5 (Credentials + JWT) · Zod · React Hook Form · Lucide · Recharts

**الحجم الفعلي:** ٣٠ صفحة · ٤ مسارات API · ١٩ جدولًا · ٧٢ مكوّنًا · ٩ ملفات إجراءات خادم · قاموس عربي من ٦٧٥ سطرًا.

**تجمّع الاتصالات:** مهلة خمول قصيرة (١٠ ثوانٍ) وسعة ١٢ — لأن Neon توقف الحوسبة الخاملة فتموت الاتصالات المحفوظة، والصفحة الرئيسية تطلق نحو اثني عشر استعلامًا متوازيًا.

**⚠️ نشر:** ضع قاعدة البيانات في **نفس منطقة** خوادم الاستضافة. قاعدة في فرانكفورت مع نشر في واشنطن تعني قطع المحيط في كل استعلام.

## ١٤) تطبيق iOS (اختياري)

الموقع مبني على الخادم بالكامل فلا يمكن تحزيمه ملفات ثابتة. الغلاف = **Capacitor** يفتح الموقع المنشور، بأيقونة واسم عربي وشاشة بداية وصفحة عربية عند انقطاع الاتصال.

البناء على **GitHub Actions بجهاز macOS**، والتوقيع بمفتاح App Store Connect مع `-allowProvisioningUpdates` فتُنشأ الشهادات تلقائيًا **بلا حاجة لجهاز ماك**.

> ⚠️ بند آبل **4.2** يرفض موقعًا مُغلَّفًا في المتجر وفي الاختبار الخارجي. **الاختبار الداخلي (حتى ١٠٠ مختبِر) بلا مراجعة** — وهو المسار المتاح. وملف الـ`.ipa` **موقَّع بحساب بعينه**، فلا يُسلَّم جاهزًا لشخص آخر.

## ١٥) الأمان

- كلمات المرور بـ bcrypt · جلسات JWT · حماية المسارات بالدور في middleware **وفي الصفحة معًا**
- رابط العودة بعد الدخول **مقيّد بالمسارات الداخلية** (`/` فقط، لا `//`) منعًا لإعادة التوجيه المفتوح
- نماذج الدخول والتسجيل بـ **`method="post"`** — بدونها، إرسال يقع قبل اكتمال التحميل يتحول إلى GET **فتظهر كلمة المرور في الرابط وسجل المتصفح**
- المساعد الشخصي مقيّد ببيانات صاحب الجلسة وحده
- ملف `.env` خارج المستودع دائمًا

## ١٦) معايير القبول

- [ ] زائر يكتب مشكلة (أو ينقر مثالًا) فيحصل على تحليل + ٣ ترشيحات **بلا تسجيل دخول** خلال ثوانٍ
- [ ] كل الترشيحات من المجال الصحيح، ولكل واحد سبب يذكر شيئًا **حقيقيًا** من خلفيته
- [ ] «احجز» يطلب الدخول ثم **يعيد المستخدم لصفحة الحجز محتفظًا بالتحليل**، والخدمة مختارة والوصف مكتوب
- [ ] الدفع ينشئ حجزًا، والخبير يقبله، فتُفتح محادثة، ويرى **ملف التحضير**
- [ ] **صندوق المشكلة لا يظهر للخبير ولا للمشرف** — يرى مكانه بطاقة تنقله للوحته
- [ ] المطابقة **تعمل بلا مفتاح API**، وتسقط للمحرك الداخلي تلقائيًا وبصمت عند أي فشل
- [ ] المساعد يجيب عن المنصة **وعن بيانات صاحب الجلسة وحده**، ولا يخترع أرقامًا، ويعطي روابط، ويعمل صوتيًا
- [ ] الشعار حرف خ وحده بلا كلمة بجانبه
- [ ] لا زر معطّل ولا رابط وهمي في التطبيق كله
- [ ] **صفر أخطاء في وحدة تحكم المتصفح**، وكل الصفحات سليمة في الوضعين الفاتح والليلي، وعلى الجوال والحاسب
- [ ] كل النصوص العربية من قاموس مركزي واحد
